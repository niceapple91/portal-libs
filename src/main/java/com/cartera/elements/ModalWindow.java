package com.cartera.elements;

import org.openqa.selenium.WebElement;

public class ModalWindow extends BaseElement {

    public ModalWindow(WebElement webElement) {
        super(webElement);
    }
}
