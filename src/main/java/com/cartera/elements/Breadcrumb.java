package com.cartera.elements;

import org.openqa.selenium.WebElement;

public class Breadcrumb extends BaseElement {

    public Breadcrumb(WebElement webElement) {
        super(webElement);
    }
}
